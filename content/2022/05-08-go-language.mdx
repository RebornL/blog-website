---
title: "Go语言学习"
date: "2022-05-08"
tags: ["Go"]
---

## init函数：Go包的初始化函数

init函数的作用：用于进行包初始化的init函数，无参数无返回值的函数

```go
func init() {
    // 包初始化逻辑
}
```

Go程序在这个包初始化的时候，可能会自动调用它的init函数，因此这些init函数可能会发生在main函数之前。

深度优先次序进行初始化
